<Window x:Class="BankingSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BankingSystem"
        xmlns:mvvm="clr-namespace:BankingSystem.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="800">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" FontFamily="Calibri" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"> Digital Banking System</TextBlock>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="3*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0 5 0 0">
                <StackPanel.Resources>
                    <Style TargetType="Button">
                        <Style.Setters>
                            <Setter Property="Width" Value="150"/>
                            <Setter Property="Height" Value="35"/>
                            <Setter Property="Margin" Value="5"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style.Setters>
                    </Style>
                </StackPanel.Resources>
                <Button Command="{Binding LoadDashboardViewCommand}">Main Dashboard</Button>
                <Button Command="{Binding LoadCustomerDashboardViewCommand}">Customer Dashboard</Button>
                <Button Command="{Binding LoadCreateAccountViewCommand}">Create Account</Button>
                <Button Command="{Binding LoadEditCustomerViewCommand}">Edit Customer</Button>
                <Button Command="{Binding LoadCloseAccountViewCommand}">Close Account</Button>
                <Button Command="{Binding LoadAccountOperationViewCommand}">Account Operations</Button>
                <Button Command="{Binding LoadCreditCardViewCommand}">Credit Card Operations</Button>
                <Button Command="{Binding LoadCertificateViewCommand}">Certificate Operations</Button>
            </StackPanel>
            <ContentControl Grid.Column="1" Content="{Binding CurrentView}"></ContentControl>
        </Grid>
    </Grid>
</Window>
