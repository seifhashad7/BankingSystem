<UserControl x:Class="BankingSystem.Views.EditCustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BankingSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" Margin="1,0,-1,0">
        <Grid.Resources>
            <Style TargetType="Label">
                <Setter Property="FontSize" Value="15" />
                <Setter Property="FontFamily" Value="Calibri" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="Margin" Value="5 0 0 0" />
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="HorizontalAlignment" Value="Left" />
                <Setter Property="Margin" Value="10 0 0 0" />
                <Setter Property="Width" Value="200" />
                <Setter Property="Height" Value="20" />
            </Style>

            <Style TargetType="RadioButton">
                <Setter Property="Width" Value="100" />
                <Setter Property="Height" Value="15" />
                <Setter Property="Margin" Value="10 0 0 0" />
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="HorizontalAlignment" Value="Left" />
            </Style>

            <Style TargetType="CheckBox">
                <Setter Property="Width" Value="auto" />
                <Setter Property="Height" Value="auto" />
                <Setter Property="Margin" Value="10 5 0 0" />
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="HorizontalAlignment" Value="Left" />
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontFamily="Calibri" FontSize="20" FontWeight="Bold" Margin="10"> Edit Customer's Info</TextBlock>
        
        <StackPanel Grid.Row="1">
            <Label>Please Enter Customer's ID you want to edit:</Label>
            <TextBox Text="{Binding Id}"/>
            
            <Label>Name:</Label>
            <TextBox Text="{Binding Name}"/>

            <Label>Age:</Label>
            <TextBox Text="{Binding Age}"/>

            <Label>National ID:</Label>
            <TextBox Text="{Binding NationalId}"/>

            <Label>Phone Number:</Label>
            <TextBox Text="{Binding PhoneNumber}"/>

            <Label>Address:</Label>
            <TextBox Text="{Binding Address}"/>

            <Label>Gender:</Label>
            <RadioButton GroupName="GenderGroup" Content="Male" IsChecked="{Binding IsMaleSelected}"/>
            <RadioButton GroupName="GenderGroup"  Content="Female" IsChecked="{Binding IsFemaleSelected}"/>
        </StackPanel>

        <WrapPanel Grid.Row="2">
            <WrapPanel.Resources>
                <Style TargetType="Button">
                    <Style.Setters>
                        <Setter Property="Width" Value="50"></Setter>
                        <Setter Property="Height" Value="20"></Setter>
                        <Setter Property="Margin" Value="10 10 0 0"></Setter>
                        <Setter Property="HorizontalAlignment" Value="Left"></Setter>
                        <Setter Property="FontSize" Value="10"></Setter>
                    </Style.Setters>
                </Style>
            </WrapPanel.Resources>
            <Button Name="SubmitButton" Command="{Binding SubmitCommand}" CommandParameter="EditMode">Submit</Button>
            <Button Name="BackButton" Command="{Binding BackCommand}">Back</Button>
        </WrapPanel>
        <ContentControl Grid.Row="1" Content="{Binding CurrentView}"></ContentControl>
    </Grid>
</UserControl>
