<UserControl x:Class="BankingSystem.Views.AccountOperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BankingSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontFamily="Calibri" FontSize="15" FontWeight="Bold" Margin="10"> Account Operation </TextBlock>

        <StackPanel Grid.Row="1" Margin="10" HorizontalAlignment="Left">
            <TextBlock FontSize="10" Margin="0 0 0 5"> Account Id</TextBlock>
            <TextBox Width="100" Text="{Binding Id}"></TextBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="10" HorizontalAlignment="Left">
            <TextBlock FontSize="10" Margin="0 0 0 5"> Amount (if any) </TextBlock>
            <TextBox Width="100" Text="{Binding Amount}"></TextBox>
        </StackPanel>

        <WrapPanel Grid.Row="3" Margin="5 10 10 10" HorizontalAlignment="Left">
            <WrapPanel.Resources>
                <Style TargetType="Button">
                    <Style.Setters>
                        <Setter Property="Width" Value="70"></Setter>
                        <Setter Property="Height" Value="20"></Setter>
                        <Setter Property="Margin" Value="5 0 0 0"></Setter>
                        <Setter Property="FontSize" Value="10"></Setter>
                    </Style.Setters>
                </Style>
            </WrapPanel.Resources>
            <Button Command="{Binding DepoistCommand}"> Depoist </Button>
            <Button Command="{Binding WithdrawCommand}"> Withdraw </Button>
            <Button Command="{Binding GetBalanceCommand}"> Get Balance </Button>
        </WrapPanel>

        <WrapPanel Grid.Row="4" Margin="10">
            <TextBlock>Balance: </TextBlock>
            <TextBlock Margin="5 0 0 0" Text="{Binding Balance}"></TextBlock>
        </WrapPanel>
    </Grid>
</UserControl>
