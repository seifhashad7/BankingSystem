<UserControl x:Class="BankingSystem.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BankingSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Load Customers" Command="{Binding LoadCustomersDataCommand}" Width="120" HorizontalAlignment="Left" Padding="5"/>

        <DataGrid Grid.Row="1" Margin="0,10,0,0" ItemsSource="{Binding Customers}" AutoGenerateColumns="True" IsReadOnly="True"/>
    </Grid>
</UserControl>
