<UserControl x:Class="BankingSystem.Views.CloseAccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BankingSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" FontFamily="Calibri" FontWeight="Bold" FontSize="20" Margin="10"> Close Account</TextBlock>
        <TextBlock Grid.Row="1" FontSize="10" Margin="10"> Account Id </TextBlock>
        <TextBox Grid.Row="2" Width="100" HorizontalAlignment="Left" Margin="10" Text="{Binding Id}"></TextBox>
        <Button Grid.Row="3" Width="60" Height="auto" HorizontalAlignment="Left" Margin="10" Command="{Binding SubmitCommand}" CommandParameter="CloseMode"> Submit </Button>
    </Grid>
</UserControl>
